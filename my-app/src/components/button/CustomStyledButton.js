import { Button } from '@chakra-ui/react';

function CustomStyledButton() {
  return (
    <Button sx={{ fontSize: '20px', padding: '10px 20px' }} colorScheme="teal">
      Custom Styled Button
    </Button>
  );
}

export default CustomStyledButton;
